{
  "name": "onboarding-data-simulator",
  "version": "1.0.0",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "onboarding",
    "data-simulator"
  ],
  "author": "",
  "license": "ISC",
  "description": "A simulator for receiving customer onboarding data and forwarding to a downstream service",
  "dependencies": {
    "axios": "^1.9.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
